defmodule MarkboxFiles.PageControllerTest do
  use MarkboxFiles.ConnCase

  test "GET /" do
    assert true
    # conn = get conn(), "/"
    # assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  end
end
